[project]
name = "ips-pilot-qlib-validation"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "pyqlib==0.9.7",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "ruff>=0.14.2",
]

[tool.uv]
package = false
